/* eslint-disable @typescript-eslint/no-unused-vars */
import React from "react";

interface SaveShareButtonProps {
  eventId: string;
  isSaved: boolean;
  source: string;
  isOwnEvent?: boolean;
}

// TEMP: fully replaced in a later task.
export default function Placeholder(_props: SaveShareButtonProps) {
  return null;
}
